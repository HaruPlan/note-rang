마크다운에 기본으로 없는 밑줄을, 밑줄 그은 것처럼 보여줘요.

## 이렇게 써요
- 밑줄 그을 글자를 `++` 두 개로 감싸요: `++꼭 확인++` → 밑줄이 그어져요.

> 💡 지금 편집 중인 그 줄에서는 `++꼭 확인++` 글자가 그대로 보여요. 커서를 다른 줄로 옮기면 밑줄로 바뀌니 걱정 마세요 — 고장이 아니라, 지금 그 줄을 편집 중이라는 표시예요.
